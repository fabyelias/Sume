# URL pública de la API desplegada en Render (sin / al final).
# En desarrollo dejala vacía o sin definir: el proxy de vite.config.ts
# ya reenvía /api a http://localhost:4000.
VITE_API_URL=https://sume-api.onrender.com
